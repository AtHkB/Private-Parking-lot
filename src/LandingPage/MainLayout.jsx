function MainLayout(porops) {
  // main container to manage imge as background in landing should be manage here
  return <div className="main-layout-container">{porops.children}</div>;
}

export default MainLayout;
